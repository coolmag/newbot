
# Music Bot (Refactored)

Это рефакторинговая версия музыкального Telegram-бота, написанного на Python с использованием библиотеки `python-telegram-bot`. Бот предназначен для поиска и скачивания музыки из различных источников прямо в чат Telegram.

## 🚀 Особенности

- **Поиск музыки**: ищет и скачивает треки с YouTube и YouTube Music.
- **Поиск аудиокниг**: находит и скачивает длинные аудиофайлы (аудиокниги) с YouTube.
- **Фоновое радио**: может воспроизводить музыку в фоновом режиме по заданным жанрам (управляется администратором).
- **Асинхронность**: полностью асинхронный код для высокой производительности.
- **Кэширование**: результаты поиска кэшируются в базе данных SQLite для уменьшения повторных запросов.
- **Модульная структура**: код разделен на логические модули для простоты поддержки и расширения.
- **Готовность к Docker**: поставляется с `Dockerfile` и `docker-compose.yml` для легкого развертывания.

## 🛠️ Технологии

- **Python 3.11**
- **python-telegram-bot**: асинхронная библиотека для Telegram Bot API.
- **yt-dlp**: мощная утилита для скачивания медиа.
- **FFmpeg**: для обработки и конвертации аудио.
- **aiosqlite**: асинхронный драйвер для работы с SQLite.
- **Docker**: для контейнеризации и развертывания.

## ⚙️ Установка и запуск

### 1. Предварительные требования

- **Python 3.11+**
- **FFmpeg**: должен быть установлен и доступен в `PATH` вашей системы.
- **Docker** и **Docker Compose** (для запуска в контейнере).

### 2. Настройка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone <your-repo-url>
    cd music-bot-refactored
    ```

2.  **Создайте и настройте `.env` файл:**
    Скопируйте ` .env.example` в `.env` и заполните его вашими данными.
    ```bash
    cp .env.example .env
    ```
    Откройте `.env` и укажите как минимум `BOT_TOKEN`.

    - `BOT_TOKEN`: токен вашего Telegram-бота (получается у @BotFather).
    - `ADMIN_IDS`: ваш Telegram ID для доступа к командам администратора (через запятую).
    - `COOKIES_FILE`: (опционально, но **настоятельно рекомендуется**) путь к файлу с cookies для обхода ограничений YouTube.
    
    **Важно для YouTube:** 
    - Без cookies файла YouTube может блокировать запросы с сообщением "Sign in to confirm you're not a bot"
    - Для получения cookies:
      1. Установите расширение браузера "Get cookies.txt LOCALLY" (для Chrome/Edge)
      2. Зайдите на youtube.com и авторизуйтесь
      3. Нажмите на расширение и экспортируйте cookies в файл `cookies.txt`
      4. Поместите файл в корень проекта или укажите полный путь в `COOKIES_FILE`
    - На Railway: загрузите файл через веб-интерфейс или укажите путь в переменной окружения

### 3. Запуск через Docker (рекомендуемый способ)

Это самый простой и надежный способ запустить бота, так как все зависимости будут установлены внутри изолированного контейнера.

```bash
docker-compose up --build
```

Чтобы остановить бота, нажмите `Ctrl+C`. Для запуска в фоновом режиме используйте:

```bash
docker-compose up -d
```

### 4. Локальный запуск (для разработки)

1.  **Создайте виртуальное окружение:**
    ```bash
    python -m venv venv
    source venv/bin/activate  # для Linux/macOS
    # или
    venv\Scripts\activate  # для Windows
    ```

2.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```
    Если вам нужны утилиты для разработки (например, линтеры), установите также:
    ```bash
    pip install -r requirements-dev.txt
    ```

3.  **Запустите бота:**
    ```bash
    python main.py
    ```

## 🗂️ Структура проекта

Все файлы находятся в корне проекта для упрощения структуры:

- `main.py`: точка входа в приложение.
- `config.py`: настройки приложения.
- `logger.py`: настройка логирования.
- `handlers.py`: обработчики команд и callback'ов бота.
- `keyboards.py`: клавиатуры для Telegram.
- `states.py`: состояние бота.
- `base.py`: базовый класс для загрузчиков.
- `youtube.py`: загрузчик для YouTube.
- `cache.py`: менеджер кэша.
- `radio.py`: сервис радио.
- `Dockerfile`: инструкция по сборке Docker-образа.
- `docker-compose.yml`: оркестрация Docker-контейнера.
- `requirements.txt`: основные зависимости проекта.
- `.env`: файл с секретными переменными (не отслеживается Git).

## 📋 Команды

- `/play <название>` или `/p <название>` - Найти и скачать трек.
- `/audiobook <название>` или `/ab <название>` - Найти и скачать аудиокнигу.
- `/radio <on|off>` - Управление радио (только для администраторов).
- `/source` или `/src` - Выбрать источник поиска (YouTube, YouTube Music).
- `/status` или `/stat` - Показать статус бота.
- `/menu` - Показать главное меню.
- `/help` - Показать справку по командам.

## 🚂 Развертывание на Railway

Railway - это платформа для развертывания приложений, которая идеально подходит для этого бота.

### Быстрый старт:

1. **Создайте аккаунт на [Railway](https://railway.app/)**

2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo" (если репозиторий уже на GitHub)
   - Или используйте "Empty Project" и подключите репозиторий позже

3. **Настройте переменные окружения:**
   - Перейдите в Settings → Variables
   - Добавьте следующие переменные:
     ```
     BOT_TOKEN=your_bot_token_here
     ADMIN_IDS=your_telegram_id
     COOKIES_FILE=cookies.txt
     ```
   - **Важно:** Для работы YouTube обязательно добавьте cookies файл:
     1. Загрузите файл `cookies.txt` в корень проекта (или создайте его через веб-интерфейс Railway)
     2. Укажите `COOKIES_FILE=cookies.txt` в переменных окружения
     3. Или используйте абсолютный путь, если файл в другой директории

4. **Развертывание:**
   - Railway автоматически обнаружит `Dockerfile` и `railway.json`
   - Нажмите "Deploy" или просто сделайте push в репозиторий
   - Railway автоматически соберет и запустит контейнер

5. **Мониторинг:**
   - Логи доступны в разделе "Deployments"
   - Можно настроить автоматический рестарт при сбоях

### Примечания для Railway:

- Railway автоматически предоставляет переменные окружения
- Файлы в `downloads/` и `cache.db` будут храниться в эфемерной файловой системе контейнера
- Для постоянного хранения кэша можно использовать Railway Volume (платная функция)
- Бот автоматически перезапустится при обновлении кода (если настроен GitHub integration)

## 🔧 Локальная разработка

Для разработки рекомендуется использовать виртуальное окружение:

```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate  # Windows

pip install -r requirements.txt
python -m src.main
```

## 📝 Лицензия

Этот проект предоставляется "как есть" для личного использования.

---

*Этот бот был разработан и отрефакторен с помощью Gemini.*
