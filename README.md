# Groove AI Bot v4.0 (Flat Structure)

Это полностью переработанная версия музыкального Telegram-бота, созданная с упором на чистоту кода, модульность и современные практики разработки на 2025 год. Бот предназначен для поиска и скачивания музыки, а также прослушивания радио в групповых чатах.

**Примечание**: Эта версия использует "плоскую" структуру файлов по требованию пользователя.

## 🚀 Особенности

- **Dependency Injection (DI)**: используется `punq` для управления зависимостями, что делает код более гибким.
- **Pydantic для настроек**: конфигурация управляется через `pydantic-settings`.
- **Оптимизированный Docker**: многостадийная сборка для уменьшения размера финального образа.
- **Структурированное логирование**: централизованная настройка логирования.

## 🛠️ Технологии

- **Python 3.11**
- **python-telegram-bot**
- **yt-dlp**
- **FFmpeg**
- **aiohttp**
- **aiosqlite**
- **pydantic**
- **punq**
- **Docker**

## ⚙️ Установка и запуск

### 1. Предварительные требования

- **Python 3.11+**
- **FFmpeg**
- **Docker** и **Docker Compose**

### 2. Настройка

1.  **Клонируйте репозиторий.**
2.  **Создайте `.env` файл** из ` .env.example` и заполните его.

### 3. Запуск через Docker (рекомендуется)

```bash
docker-compose up --build
```

### 4. Локальный запуск (для разработки)

```bash
pip install -r requirements.txt
python main.py
```

## 🗂️ Структура проекта

Все файлы находятся в корневой директории.

- `main.py`: точка входа
- `config.py`: настройки
- `container.py`: DI-контейнер
- `logging.py`: настройка логирования
- `models.py`: модели данных
- `cache.py`: сервис кэширования
- `downloaders.py`: загрузчики
- `radio.py`: сервис радио
- `handlers.py`: обработчики команд
- `keyboards.py`: клавиатуры
- `constants.py`: константы
- `Dockerfile`
- `docker-compose.yml`
- `requirements.txt`

## 📋 Команды

- `/start`, `/help` - Справка.
- `/play <название>` - Найти и скачать трек.
- `/menu` - Главное меню.
- `/admin` - Админ-панель.

---

*Этот бот был полностью переработан с помощью Gemini.*
